package oop.abstraction;

public interface BoostEngine extends AutoPilot {
    //all the methods from auto pilot is now in boost engine  because we extend AutoPilot in BoostEngine
    public void boostEngine();
}
